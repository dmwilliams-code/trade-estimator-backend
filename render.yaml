services:
  - type: cron
    name: estimateai-seo-report
    runtime: node
    schedule: "0 8 * * 1"
    buildCommand: npm install
    startCommand: node scripts/gsc-claude-analyser.js
    region: frankfurt